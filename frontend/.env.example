# Local development defaults. Copy to .env.local to customize.
# Backend REST base URL used by the frontend.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Backend WebSocket base URL used by the frontend.
NEXT_PUBLIC_WS_URL=ws://localhost:8000
