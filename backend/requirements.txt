# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0

# FastAPI and WebSocket
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data validation
pydantic>=2.0.0

# YOLO11 detector (second detector, Classical CV remains default)
# CPU-only torch is sufficient (install CPU wheel explicitly if needed).
torch>=2.4.0,<3.0.0
ultralytics>=8.2.0,<9.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
