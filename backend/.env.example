# Local development defaults for the FastAPI backend.
# Comma-separated list of allowed CORS origins (explicit, never "*"
# when credentials are enabled). Defaults preserve local development
# with the Next.js dev server on port 3000.
FSOC_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
